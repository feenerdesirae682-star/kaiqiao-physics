# 学习示例来源规则

当前版本仍只发布学习示例，不宣称任何一道题是已核对的官方原卷原题。2026-09-25 起增加"维护者题库核对"一档，它高于"出处待核对"，但明确低于官方原卷核对。

## 四种来源状态

- `source.status: unverified`：由原有题干整理、来源未核对。历史年份和卷名存入 `source.originalLabel`，仅为维护者核对线索，不参与学生页面展示或搜索。
- `source.status: teaching-example`：确认由本站编写的教学示例，不得携带 `originalLabel`。不能把待核对题目直接改为本站原创。
- `source.status: bank-original`：已与维护者自有题库中标注为同一试卷的题目逐字核对，题干、选项、条件、答案实质一致。页面标签"学习示例 · 与维护者题库一致"。
- `source.status: bank-adapted`：维护者题库中能定位母题，本站确有改写，全部差异写在 `source.adaptation`，改写后的答案已重新验算。页面标签"学习示例 · 据维护者题库改编"。

两种 `bank-*` 状态必须同时携带：
- `source.originalLabel`：仍是维护者线索，仍不展示给学生；
- `source.bankRef`：`{ bank, id, label, checkedOn }`，指明题库名称、题库内条目 id、题库对该题的卷名标注、核对日期；
- `bank-adapted` 还必须有非空的 `source.adaptation`，逐项写明与题库版本的差异。

## 维护者题库是什么、不是什么

维护者题库指 `D:\工作内容\高考物理真题库` 解析出的 `output/questions.json`（4747 道，2004–2025 各省卷），其原始素材是按章节汇编的 docx，文档内带有网络题库来源字样，属于网络汇编，**不是考试院或教育部考试院发布的原卷，也不是正式出版物**。因此：

- `bank-*` 只表示"与维护者题库一致/据其改编"，页面提示必须说明题库为网络汇编、不等于官方原卷；
- 任何页面、生成文件、文档都不得因此把题目称为"真题""原卷原题""已核对原卷"；
- 取得官方原卷后，`bank-*` 题目应按 `SOURCE_VERIFICATION.md` 的流程再核一次；是否新增官方核对状态另行决定；
- 题库里找不到对应题、或只有主题相近的另一道题，不能记为 `bank-*`；相似度、年份地区吻合都不构成依据。

## 通用禁止项

- 禁止在题目顶层恢复 `year`、`province`、`sourceStatus`。来源提示由统一函数生成，未知状态在界面上仍显示"出处待核对"，自动检查则报错。
- 自动检查明确拒绝 `verified`、`verified-original`、`verified-adapted` 等未启用的状态，避免仅改状态字符串就冒充完成核对。
- 静态资源本来公开，因此"仅供维护者"表示不渲染到学生界面，并不表示这些字段属于保密数据。

## 核对记录

- 官方来源核对流程与台账：`SOURCE_VERIFICATION.md`、`SOURCE_VERIFICATION_BATCH1.md`（第一批证据调查，16 道均未取得官方文件）。
- 维护者题库核对记录：`SOURCE_VERIFICATION_BATCH2.md`（题库文件 SHA-256、逐题比对与差异）。

## 日后官方核对要求

逐题取得原卷或可信发布页，登记年份、完整卷名、题号/页码、可访问的来源链接、核对人和日期；对照题干、选项、图示、答案及改写差异。来源本身真实不等于本站改写题等同原题。已改写题应标注"根据原题改编"及差异，不能标"原卷原题"。

## 自动检查边界

检查能证明状态合法、必要字段齐全、线索未丢失、页面显示提示，不能证明历史标签正确、题库版本与官方原卷一致、题目答案正确或版权授权。
